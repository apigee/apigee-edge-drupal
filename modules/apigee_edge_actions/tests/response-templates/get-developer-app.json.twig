{
  "appFamily": "default",
  "appId": "{{ app.uuid }}",
  "attributes": [
    {
      "name": "DisplayName",
      "value": "{{ app.displayName }}"
    }
  ],
  "developerId": "{{ app.developerId }}",
  "name": "{{ app.name }}",
  "status": "{{ app.status }}"
}
