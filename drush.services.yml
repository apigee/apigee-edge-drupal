services:
  apigee_edge.commands:
    class: \Drupal\apigee_edge\Commands\ApigeeEdgeCommands
    arguments: ['@apigee_edge.cli', '@apigee_edge.edge_connection_util']
    tags:
      - { name: drush.command }
